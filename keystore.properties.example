# Copy to keystore.properties (gitignored) at project root.
# Or omit this file and set RELEASE_STORE_FILE (optional), RELEASE_STORE_PASSWORD, RELEASE_KEY_PASSWORD in the environment.
storeFile=android.keystore
keyAlias=android
storePassword=
keyPassword=
